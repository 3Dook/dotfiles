return {
    {
      'nvim-telescope/telescope.nvim', 
      tag = '0.1.5',
      dependencies = { 'nvim-lua/plenary.nvim' },
      -- need to config the preview button todo
      config = function()
      end
    }
}
